import { DropZone, DropPicture, Uploods, Provider, Context } from '..'

it('exports DropZone', () => {
  expect(DropZone).not.toBeUndefined()
})

it('exports DropPicture', () => {
  expect(DropPicture).not.toBeUndefined()
})

it('exports Uploods', () => {
  expect(Uploods).not.toBeUndefined()
})

it('exports Provider', () => {
  expect(Provider).not.toBeUndefined()
})

it('exports Context', () => {
  expect(Context).not.toBeUndefined()
})
